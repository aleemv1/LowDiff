{
  "name": "lowdiff",
  "private": true,
  "version": "0.0.1",
  "description": "AI review annotations layered over GitHub pull-request diffs",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b",
    "build": "npm run build --workspace @lowdiff/extension"
  },
  "devDependencies": {
    "happy-dom": "^16.8.1",
    "playwright": "^1.62.1",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "@types/prismjs": "^1.26.6",
    "prismjs": "^1.30.0"
  }
}
